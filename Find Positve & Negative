#include <stdio.h>

int main()
{
    int number;
    printf("Enter the number:");
    scanf("%d",&number);
    if(number<=0)
    {
    if(number==0)
    printf("number is zero");
    else 
    printf("number is negative");
    }
    else
    printf("number is positive");
    return 0;
}
